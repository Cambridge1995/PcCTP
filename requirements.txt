# =============================================================================
# PcCTP - CTP PC版行情API Python绑定
# Python 依赖包配置文件
# =============================================================================

# -----------------------------------------------------------------------------
# 核心依赖 (必需)
# -----------------------------------------------------------------------------

# NumPy - 数组零拷贝支持
# 用于订阅行情时的合约数组传递（直接访问底层数据指针，零拷贝）
numpy>=1.20.0

# -----------------------------------------------------------------------------
# 构建依赖 (编译模块时需要)
# -----------------------------------------------------------------------------

# nanobind - 现代 C++/Python 绑定库
# 如果使用 USE_NANOBIND=ON 构建选项时需要
# 安装方法: pip install nanobind
nanobind>=1.8.0

# -----------------------------------------------------------------------------
# 开发和测试依赖 (可选，用于测试和开发)
# -----------------------------------------------------------------------------

# akshare - 金融数据接口
# 用于获取交易日历，判断7x24和实时行情阶段（仅测试脚本使用）
akshare>=1.9.0
